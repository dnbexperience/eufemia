# DNB **Eufemia** design system packages

In here we have several packages with different purposes. `@dnb/eufemia` is the package to build the NPM version of the UI components. `dnb-design-system-portal` is the main website to showcase both the **Design System Principles and rules** and the **@dnb/eufemia** components, styles and extensions.
