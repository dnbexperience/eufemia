/**
 * Factory Test
 *
 */

import { runFactory } from '../makeDemosFactory'

describe('Factory runs as expected :)', () => {
  // TODO: Make real tests
  it('runFactory is a function', async () => {
    expect(typeof runFactory === 'function').toBe(true)
  })
})
