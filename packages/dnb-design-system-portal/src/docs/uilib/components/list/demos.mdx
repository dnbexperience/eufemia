---
showTabs: true
---

import * as Examples from './Examples'

## Demos

### Rows with Start, Center, End

<Examples.RowsWithSlots />

### Navigable item

<Examples.NavigableItem />

### With anchor

List items containing [Anchor](/uilib/components/anchor) links.

<Examples.WithAnchor />

### Accordion

Expandable list items using `List.Item.Accordion` with `Header` and `Content`. Use the `open` prop to set the initial open state.

<Examples.Accordion />

### Simple rows

<Examples.SimpleRows />

### With form elements

<Examples.WithFormElements />

### Selected state

<Examples.SelectedState />

### Separated lists

Use the `separated` prop on `List.Container` to add row gap between list items.

<Examples.SeparatedLists />

### Pending state

Use the `pending` prop on `List.Item.Content` or `List.Item.Navigate` to show a skeleton overlay. Click and keyboard are disabled while pending.

<Examples.PendingState />

### Progress indicator

A single list item with a circular progress indicator in `List.Item.Start`.

<Examples.ProgressIndicatorRow />

### Skeleton

Use the `skeleton` prop on `List.Item.Content` or `List.Item.Navigate` to show a skeleton overlay while content is loading.

<Examples.SkeletonState />

### With avatar

Use [Avatar](/uilib/components/avatar) in `List.Item.Start` as the left icon.

<Examples.WithAvatar />
