---
showTabs: true
---

import * as Examples from './Examples'

## Demos

### Rows with Start, Center, End

This example demonstrates different cell layouts and their placement.

<Examples.RowsWithSlots />

### Navigable item

<Examples.NavigableItem />

### Navigable item with href

Use the `href` prop on `List.Item.Action` to render a native link. Use `target` and `rel` for external links (e.g. `target="_blank"` with `rel="noopener noreferrer"`).

<Examples.NavigableItemWithHref />

### With anchor

List items containing [Anchor](/uilib/components/anchor) links.

<Examples.WithAnchor />

### Accordion

Expandable list items using `List.Item.Accordion` with optional `icon` and `title` props and `List.Item.Accordion.Content` for the expandable section. Use the `open` prop to set the initial open state.

<Examples.Accordion />

### Footer with buttons

Use `List.Cell.Footer` to place actions such as [Button](/uilib/components/button) in the list row.

<Examples.FooterWithButtons />

### Responsive Grid Layout

Using [Grid.Container](/uilib/layout/grid/container) with [Grid.Item](/uilib/layout/grid/item) for a 12-column responsive grid.

<Examples.GridColumns />

### Separated lists

Use the `separated` prop on `List.Container` to add row gap between list items.

<Examples.SeparatedLists />

### Dynamic list

<Examples.SimpleRows />

### With DateFormat

Use [DateFormat](/uilib/components/date-format) in `List.Cell.Start` to show dates in the list row.

<Examples.WithDateFormat />

### With form elements

<Examples.WithFormElements />

### With avatar

Use [Avatar](/uilib/components/avatar) in `List.Cell.Start` as the left content.

<Examples.WithAvatar />

### Selected state

<Examples.SelectedState />

### With custom background color

<Examples.BackgroundColor />

### Pending state

Use the `pending` prop on `List.Item.Basic` or `List.Item.Action` to show a skeleton overlay. Click and keyboard are disabled while pending.

<Examples.PendingState />

### Progress indicator

A single list item with a circular progress indicator in `List.Cell.Start`.

<Examples.ProgressIndicatorRow />

### Skeleton

Use the `skeleton` prop on `List.Item.Basic` or `List.Item.Action` to show a skeleton overlay while content is loading.

<Examples.SkeletonState />
