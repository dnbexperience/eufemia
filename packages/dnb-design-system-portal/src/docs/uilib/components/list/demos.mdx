---
showTabs: true
---

import * as Examples from './Examples'

## Demos

### Rows with Start, Center, End

<Examples.RowsWithSlots />

### Navigable item

<Examples.NavigableItem />

### Navigable item with href

Use the `href` prop on `List.Item.Variant.Navigate` to render a native link. Use `target` and `rel` for external links (e.g. `target="_blank"` with `rel="noopener noreferrer"`).

<Examples.NavigableItemWithHref />

### With anchor

List items containing [Anchor](/uilib/components/anchor) links.

<Examples.WithAnchor />

### Accordion

Expandable list items using `List.Item.Variant.Accordion` with `Header` and `Content`. Use the `open` prop to set the initial open state.

<Examples.Accordion />

### Simple rows

<Examples.SimpleRows />

### With form elements

<Examples.WithFormElements />

### Selected state

<Examples.SelectedState />

### Separated lists

Use the `separated` prop on `List.Container` to add row gap between list items.

<Examples.SeparatedLists />

### Pending state

Use the `pending` prop on `List.Item.Variant.Content` or `List.Item.Variant.Navigate` to show a skeleton overlay. Click and keyboard are disabled while pending.

<Examples.PendingState />

### Progress indicator

A single list item with a circular progress indicator in `List.Item.Layout.Start`.

<Examples.ProgressIndicatorRow />

### Skeleton

Use the `skeleton` prop on `List.Item.Variant.Content` or `List.Item.Variant.Navigate` to show a skeleton overlay while content is loading.

<Examples.SkeletonState />

### With avatar

Use [Avatar](/uilib/components/avatar) in `List.Item.Layout.Start` as the left icon.

<Examples.WithAvatar />
