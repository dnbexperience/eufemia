---
showTabs: true
---

## Import

```tsx
import { List } from '@dnb/eufemia'
```

## Description

`List` is a layout component for displaying rows of content. Use `List.Container` as the wrapper and `List.Item.Variant.Content`, `List.Item.Variant.Navigate`, or `List.Item.Variant.Accordion` for each row.

- **List.Container** – Provides list context (e.g. variant) and wraps items in a vertical flex layout.
- **List.Item.Variant.Content** – A single row with horizontal layout. Supports selected state, variant override, and loading states via `pending` (skeleton overlay) or `skeleton` (text placeholder).
- **List.Item.Variant.Navigate** – Like Content but clickable (Enter/Space support) with a chevron icon. Use `iconPosition="left"` or `"right"` (default) to place the chevron. Supports `pending` to show a loading overlay and disable interaction.
- **List.Item.Variant.Accordion** – Expandable row with `List.Item.Variant.Accordion.Header` and `List.Item.Variant.Accordion.Content`. Use `open` for initial state, `iconPosition="left"` or `"right"` for chevron placement, and optional `id` for ARIA. Supports `pending` to disable toggling.
- **List.Item.Layout.Start**, **List.Item.Layout.Center**, **List.Item.Layout.End** – Slots inside Content/Navigate/Accordion for start, middle, and end.
- **List.Item.Layout.Title** – Title slot (grows); **List.Item.Layout.Icon** – Icon slot (medium size).

All item components support [Space](/uilib/layout/space/properties) props (`top`, `bottom`, etc.) and forward standard HTML attributes.

## Relevant links

- [Figma](https://www.figma.com/design/cdtwQD8IJ7pTeE45U148r1/%F0%9F%92%BB-Eufemia---Web?node-id=47195-2954)
- [Source code](https://github.com/dnbexperience/eufemia/tree/main/packages/dnb-eufemia/src/components/list)
- [Docs code](https://github.com/dnbexperience/eufemia/tree/main/packages/dnb-design-system-portal/src/docs/uilib/components/list)

## Basic usage

```jsx
import { List } from '@dnb/eufemia'

render(
  <List.Container>
    <List.Item.Variant.Content>Simple row</List.Item.Variant.Content>
    <List.Item.Variant.Content selected>Selected row</List.Item.Variant.Content>
    <List.Item.Variant.Content>
      <List.Item.Layout.Start>Start</List.Item.Layout.Start>
      <List.Item.Layout.Center>Center</List.Item.Layout.Center>
      <List.Item.Layout.End>End</List.Item.Layout.End>
    </List.Item.Variant.Content>
    <List.Item.Variant.Navigate onClick={() => console.log('Clicked')}>
      <List.Item.Layout.Title>Title</List.Item.Layout.Title>
      <List.Item.Layout.End>Value</List.Item.Layout.End>
    </List.Item.Variant.Navigate>
    <List.Item.Variant.Accordion>
      <List.Item.Variant.Accordion.Header>
        <List.Item.Layout.Title>Expandable</List.Item.Layout.Title>
      </List.Item.Variant.Accordion.Header>
      <List.Item.Variant.Accordion.Content>
        <p>Content when expanded.</p>
      </List.Item.Variant.Accordion.Content>
    </List.Item.Variant.Accordion>
  </List.Container>
)
```

## Loading states

- **pending** – On `List.Item.Variant.Content` or `List.Item.Variant.Navigate`: shows a skeleton overlay and disables pointer events. On `List.Item.Variant.Navigate`, click and keyboard are disabled (`tabIndex={-1}`, `aria-disabled`). Use while data is loading.
- **skeleton** – On `List.Item.Variant.Content` or `List.Item.Variant.Navigate`: applies skeleton font styling (text placeholder) without the full overlay. Use for a lighter loading indication.

## Accessibility

- **List.Item.Variant.Navigate** uses `role="button"` so assistive technologies announce it as a button. It is focusable (`tabIndex={0}`) and activates on Enter and Space. When `pending` is true, it is not focusable and has `aria-disabled="true"`. You can override the role via the `role` prop (e.g. `role="link"`).
- **List.Item.Variant.Accordion** exposes full ARIA for expand/collapse: the header has `id`, `aria-controls`, and `aria-expanded`; the content region has `id`, `aria-labelledby`, `aria-hidden`, and `aria-expanded`. Pass an `id` prop for stable references, or leave it unset for an auto-generated id. When `pending` is true, the header is not focusable and has `aria-disabled="true"`.
- Use `aria-label` or other ARIA attributes on the container or items when needed for screen readers.
