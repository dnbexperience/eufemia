---
showTabs: true
redirect_from:
  - /uilib/components/number/properties
---

import TranslationsTable from 'dnb-design-system-portal/src/shared/parts/TranslationsTable'
import PropertiesTable from 'dnb-design-system-portal/src/shared/parts/PropertiesTable'
import {
  NumberFormatNumberProperties,
  NumberFormatCurrencyProperties,
  NumberFormatPercentProperties,
  NumberFormatPhoneProperties,
  NumberFormatBANProperties,
  NumberFormatNINProperties,
  NumberFormatOrganizationProperties,
} from '@dnb/eufemia/src/components/number-format/NumberFormatDocs'

## Properties

`NumberFormat` is only exposed as a namespace. Pick the variant you need – this keeps the bundle small because only the variants you import are included.

### `NumberFormat.Number`

<PropertiesTable props={NumberFormatNumberProperties} />

### `NumberFormat.Currency`

<PropertiesTable props={NumberFormatCurrencyProperties} />

### `NumberFormat.Percent`

<PropertiesTable props={NumberFormatPercentProperties} />

### `NumberFormat.PhoneNumber`

<PropertiesTable props={NumberFormatPhoneProperties} />

### `NumberFormat.BankAccountNumber`

Norwegian bank account number (e.g. `2000 12 34567`).

<PropertiesTable props={NumberFormatBANProperties} />

### `NumberFormat.NationalIdentityNumber`

Norwegian national identification number (e.g. `180892 12345`).

<PropertiesTable props={NumberFormatNINProperties} />

### `NumberFormat.OrganizationNumber`

Norwegian organization number (e.g. `123 456 789`). Screen readers read digit by digit.

<PropertiesTable props={NumberFormatOrganizationProperties} />

## Translations

<TranslationsTable localeKey="NumberFormat" />
