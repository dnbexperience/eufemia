---
title: 'Base toggle components'
order: 1
---

import ListBaseToggleComponents from './ListBaseToggleComponents'

# Base toggle components

Base toggle components are basic field types that allow the user to toggle between two values, such as `true` and `false`.

<ListBaseToggleComponents />
