---
showTabs: true
hideInMenu: true
---

## Description

`Iterate.RemoveButton` connects to the array of a surrounding `Iterate.Array` and removes the entry when clicked.

```tsx
import { Iterate, Field } from '@dnb/eufemia/extensions/forms'

render(
  <>
    <Iterate.Array path="/">
      <Field.Name.Last itemPath="/name" />
    </Iterate.Array>

    <Iterate.RemoveButton text="Remove entry" path="/" pushValue={{}} />
  </>,
)
```
