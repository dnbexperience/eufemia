---
showTabs: true
---

import DataValueReadwriteProperties from '../../data-value-readwrite-properties.mdx'

## Properties

<DataValueReadwriteProperties type="string" />

### Component-specific props

| Property           | Type      | Description                                                                                                           |
| ------------------ | --------- | --------------------------------------------------------------------------------------------------------------------- |
| `placeholder`      | `string`  | _(optional)_ use to decide which placeholder to use. Can be `dashed`, `letters`, `space` or `none`. Default: `dashed` |
| `keep_placeholder` | `boolean` | _(optional)_ use to make the `placeholder` persist during focus. Default: `true`                                      |
