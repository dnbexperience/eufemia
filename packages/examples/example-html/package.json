{
  "name": "example-html",
  "description": "Vanilla HTML example",
  "license": "SEE LICENSE IN LICENSE FILE",
  "repository": {
    "type": "git",
    "url": "https://github.com/dnbexperience/eufemia.git"
  },
  "version": "1.0.0",
  "scripts": {
    "start": "live-server static --watch --no-browser --cors --port=8080",
    "umd": "live-server static/umd --watch --no-browser --cors --port=8080"
  },
  "dependencies": {},
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
